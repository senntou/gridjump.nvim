このディレクトリに、gridjump.nvimというプラグインを作成したい。
hop.nvimやjumpcursor.vimなどのプラグインに影響を受けて作られるプラグインです。

このプラグインは、 `:GridJump` コマンドを追加します。
:GridJump を使用すると、同じバッファ内の一番左の列に 'abcdef...' の順で上から赤文字（色は設定可能にすべき）が表示されます。
同じように、一番上の行（もしくは下の行でも良いかも？）に'abcdef...'の順で赤文字が表示されます。
ここで、左上の文字 'a' は重複して重なっているような状況を想定しています。
文字の並び順は、'a...zA...Z0...9@...!' みたいな感じを想定していますが、これも設定で変更可能にするべきです。 
何らかのデフォルト並び順を持つこととします。

つまり、ここで行と列に対応する文字が1つずつ決定されます。
:GridJump 発動後、行->列 の順にキーを押すことで、対応する座標に飛ぶことができます。
例えば、 :GridJump ce であれば、3行5列目にジャンプします。

ここでUX改善のため、行を選択するときには行のindex表示を、列を選択するときには列のindex表示を強調表示ができるとなお良いです。

また、選択可能なオプションとして、以下はほしいです。
1. index_rows : 列のindexを表示する行の番号。デフォルトでは[1]。例えば[1, 10]にすると、1行目と10列目に表示される。ただし、できれば”最後の行”とかみたいな指定の仕方もできてほしいので、仕様をどうするか迷うところ、、、。
2. index_cols : 上の列と行逆バージョン
3. index_style : indexの文字の色とか、強調表示時の色とか、強調表示されていないときの色とか
4. index_chars : 'abcdef...' とか、 'qwertyuiop...' みたいなindexの並び順。設定されていない文字列は、デフォルトのものを使うようにしたい。
例えば設定が'qwety'だけ だったら、'qwertyabcdf...'みたいに、qwertyのあとにデフォルト並び順を持ってくる。




